---
import { urgencyClass } from '../status.ts';
interface Props { tags: string[]; }
const { tags } = Astro.props;
// Labels have already been normalized and ordered at the status boundary.
---
{tags.map((t) => <span class={`u ${urgencyClass(t)}`}>{t}</span>)}
